# Морской бой

## Запуск

Нужен Node.js 18+.

```bash
npm install
npm start
```

Или из папки `back`:

```bash
cd back
npm install
npm start
```

Открой http://localhost:3000

**Важно:** не открывай `front/index.html` напрямую в браузере — нужен сервер (socket.io).
